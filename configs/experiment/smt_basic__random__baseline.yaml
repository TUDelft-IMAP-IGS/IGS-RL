# @package _global_
#
# SMT Basic — Random baseline
#
defaults:
    - override /method: random
    - override /scenario: smt_basic
    - override /reward: default

exp_name: "smt_basic__random__baseline"
