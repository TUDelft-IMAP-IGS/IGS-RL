# @package _global_
defaults:
    - override /method: ppo_transformer_joint
    - override /scenario: smt_basic_marshalling_early
    - override /reward: time_only

exp_name: "smt_basic_no_early_install__ppo_transformer__time"
