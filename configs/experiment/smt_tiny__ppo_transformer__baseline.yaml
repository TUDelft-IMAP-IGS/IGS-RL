# @package _global_
#
# SMT Tiny — PPO + Transformer, joint action, default reward
# Minimal scenario for quick iteration and debugging.
#
defaults:
    - override /method: ppo_transformer_joint
    - override /scenario: smt_tiny
    - override /reward: default

exp_name: "smt_tiny__ppo_transformer__baseline"
