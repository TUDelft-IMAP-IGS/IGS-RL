# @package _global_
#
# SMT IS — PPO + Transformer, joint action, default reward
#
defaults:
    - override /method: ppo_transformer_joint
    - override /scenario: smt_is
    - override /reward: default

exp_name: "smt_is__ppo_transformer__baseline"
