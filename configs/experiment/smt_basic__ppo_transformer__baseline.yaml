# @package _global_
#
# SMT Basic — PPO + Transformer, joint action, default reward
#
defaults:
    - override /method: ppo_transformer_joint
    - override /scenario: smt_basic
    - override /reward: default

exp_name: "smt_basic__ppo_transformer__baseline"
