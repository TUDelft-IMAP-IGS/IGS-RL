# @package _global_
#
# SMT Basic — PPO + Transformer, marshalling close + no install window,
# reward tuned to discourage using marshalling yard.
#
defaults:
    - override /method: ppo_transformer_joint
    - override /scenario: smt_basic_marshalling_no_window
    - override /reward: time_only

exp_name: "smt_basic__ppo_transformer__no_marshalling"

env:
    sim:
        activities:
            installations:
                Bokalift:
                    installation_site: 40.0
