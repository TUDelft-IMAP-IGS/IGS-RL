# @package _global_
#
# SMT Movable Boka — PPO + Transformer, joint action, time + travel costs
# Optimal policy: Boka stays at IS while HTV delivers directly.
#
defaults:
    - override /method: ppo_transformer_joint
    - override /scenario: smt_movable_boka
    - override /reward: time_and_travel

exp_name: "smt_movable_boka__ppo_transformer__time_and_travel"

env:
    sim:
        use_business_rules: False
