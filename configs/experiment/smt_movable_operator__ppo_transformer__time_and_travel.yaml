# @package _global_
#
# SMT Movable Operator — PPO + Transformer, joint action, time + travel costs
# Optimal policy: Operator stays at IS while HTV delivers directly.
#
defaults:
    - override /method: ppo_transformer_joint
    - override /scenario: smt_movable_operator
    - override /reward: time_and_travel

exp_name: "smt_movable_operator__ppo_transformer__time_and_travel"

env:
    sim:
        use_business_rules: False
