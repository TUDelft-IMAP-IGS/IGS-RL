# @package _global_
#
# SMT Movable Operator — Random baseline
#
defaults:
    - override /method: random
    - override /scenario: smt_movable_operator
    - override /reward: default

exp_name: "smt_movable_operator__random__baseline"

random:
    num_episodes: 100000
