# @package _global_
#
# SMT Basic — PPO + Transformer, marshalling + early window,
# milestone reward with elapsed time and storage costs.
#
defaults:
    - override /method: ppo_transformer_joint
    - override /scenario: smt_basic_marshalling_early
    - override /reward: time_and_storage

exp_name: "smt_basic__ppo_transformer__time_and_storage"
