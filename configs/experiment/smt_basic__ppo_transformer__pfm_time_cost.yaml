# @package _global_
#
# SMT Basic — PPO + Transformer, PFM multi-objective (Time 80%, Cost 20%)
# Uses Preference Function Modeling for principled multi-objective scalarization.
#
defaults:
    - override /method: ppo_transformer_joint
    - override /scenario: smt_basic_marshalling_early
    - override /reward: pfm_time_and_cost

exp_name: "smt_basic__ppo_transformer__pfm_time_cost"
