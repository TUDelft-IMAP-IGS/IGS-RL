# @package _global_
#
# SMT Movable Boka — PPO + Transformer, joint action, time cost only
# Optimal policy: Boka shuttles MY <-> IS while HTV feeds FY -> MY.
#
defaults:
    - override /method: ppo_transformer_joint
    - override /scenario: smt_movable_boka
    - override /reward: time_only

exp_name: "smt_movable_boka__ppo_transformer__time_only"
