# @package _global_
#
# SMT Movable Operator — same as smt_basic but installation_vessel can move between sites
#
defaults:
    - override /env: smt
    - override /env/sim: smt_movable_operator

env:
    sim:
        vessels:
            installation_vessel:
                movable: true
